services:
  - type: web
    name: eurobridge-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: node backend/src/server.js
    envVars:
      - key: NODE_ENV
        value: production
